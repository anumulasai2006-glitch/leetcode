class Solution {
    public long maxTotalValue(int[] nums, int k) {
        Arrays.sort(nums);
        int n=nums.length;
        int min=nums[0];
        int max=nums[n-1];
        return 1L*k*(max-min);
    }
}
